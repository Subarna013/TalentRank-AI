import streamlit as st


def hero():
    """Minimal welcome header — replaces the large hero banner to match the admin design."""
    # The welcome header is now rendered directly in dashboard.py
    # This function is kept for backward compatibility but does nothing heavy
    pass
